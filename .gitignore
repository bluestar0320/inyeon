# 의존성
node_modules/

# Next.js
.next/
out/
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
/playwright/.cache/

# 환경 변수
.env
.env.local
*.env

# AI 코딩 도구가 설치한 스킬(uipro init 등). 각자 로컬에서 받는다 — 4MB가 넘고
# 저장소에 둘 이유가 없다. .claude/ 전체가 아니라 skills/만 막아서, 나중에
# 프로젝트 설정(.claude/settings.json)을 공유하고 싶을 때 길이 막히지 않게 한다.
.claude/skills/
.playwright-mcp/

# OS / 편집기
.DS_Store
.vscode/
.idea/

# 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*
